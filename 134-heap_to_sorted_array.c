#include "binary_trees.h"

/**
 * heap_to_sorted_array - converts a Binary
 * to a sorted array
 * @heap: a pointer to the root
 * @size: an address to store
 *
 * Return: the generated array
 *         NULL on failure
 */
int *heap_to_sorted_array(heap_t *heap, size_t *size)
{

}
