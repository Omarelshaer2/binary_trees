#include "binary_trees.h"

/**
 * heap_extract - extracts root 
 * @root: a double pointer to the root we want to
 *
 * Return: the value stored in the root node
 *         0 on failure
 */
int heap_extract(heap_t **root)
{

}
